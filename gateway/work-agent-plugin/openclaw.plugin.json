{
  "id": "work-agent",
  "name": "Work Agent",
  "description": "Gmail/Calendar/Drive/Telegram — search, send, schedule, report.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "mcpServerUrl": {
        "type": "string",
        "description": "URL of google-mcp-sidecar (e.g. http://google-mcp-sidecar.railway.internal:8000)"
      },
      "dbUrl": {
        "type": "string",
        "description": "PostgreSQL connection string for Telegram message index"
      }
    }
  },
  "uiHints": {
    "mcpServerUrl": {
      "label": "Google MCP Server URL",
      "help": "HTTP endpoint of the google-mcp-sidecar service"
    },
    "dbUrl": {
      "label": "Postgres URL",
      "help": "Connection string for Telegram message index (shared with telegram-sidecar)"
    }
  }
}
